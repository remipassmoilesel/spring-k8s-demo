apiVersion: v1
kind: Service
metadata:
  name: {{ template "nats-helm-chart.fullname" . }}
  labels:
    app: {{ template "nats-helm-chart.name" . }}
    chart: {{ template "nats-helm-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "nats-helm-chart.name" . }}
    release: {{ .Release.Name }}
